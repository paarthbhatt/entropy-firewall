apiVersion: v1
kind: Secret
metadata:
  name: entropy-secrets
  namespace: entropy
type: Opaque
stringData:
  # CHANGE THESE VALUES IN PRODUCTION!
  master-api-key: "ent-production-master-key-change-me"
  openai-api-key: "sk-..."
  db-host: "entropy-db"
  db-password: "entropy_password"
  redis-url: "redis://entropy-redis:6379/0"
